# 配置模板：复制此文件为 .env 并填写真实值
#   cp .env.example .env
# .env 已被 .gitignore 忽略，不会被提交

# 在此登记技能需要的配置项（变量名 + 注释说明用途）：

# GLM_API_KEY=         # 智谱开放平台密钥（mp3-to-article 技能的 GLM-ASR 语音转文本用）
# OPENAI_API_KEY=      # 调用 OpenAI API 的密钥
# GITHUB_TOKEN=        # GitHub 访问令牌
